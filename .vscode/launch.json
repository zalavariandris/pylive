{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run as Module",
            "type": "python",
            "request": "launch",
            "module": "experiments.camera_spy.examples.camera_spy_v4",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}